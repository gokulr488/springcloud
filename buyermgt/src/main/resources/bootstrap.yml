spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config
  application:
    name: buyermgt
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    registerWithEureka: true
    fetchRegistry: true
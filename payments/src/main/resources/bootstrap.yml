spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config
  application:
    name: payments-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    registerWithEureka: true
    fetchRegistry: true